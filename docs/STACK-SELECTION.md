# Stack selection

This export was created for the **typescript-node** profile — see [the profile guide](../profiles/typescript-node.md).

The full multi-stack comparison matrix lives in the upstream Sparkwright kit
(`docs/STACK-SELECTION.md`); it is omitted here because the other stack profiles
are not included in a single-profile export.
