# GitHub CI check registration

This temporary pull request exists only to register the repository's `ci` and
`control-plane-ratification` check names before branch protection is enabled.
It is an ordinary documentation-only change, so the custom ratification check
must conclude `success`. The pull request will be closed without merging after
the live checks finish.
